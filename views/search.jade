extends layout

block content
  nav.navbar.navbar-custom.navbar-fixed-top(role='navigation')
      .container
        .collapse.navbar-collapse.navbar-right.navbar-main-collapse
          ul.nav.navbar-nav
            li.hidden
              a(href='#page-top')
            li
              a.page-scroll(href='#about') The App
            li
              a.page-scroll(href='#trial')
                span.light Start a grant
            li
              a.page-scroll(href='#contact') Contact Us
    header.intro
      .intro-body
        .container
          .row
            .col-md-8.col-md-offset-2
                h1.brand-heading
                  b
                    | S
                    span#title earch
                form.search_form(action='/search', method='post', name='search_form')
                  input(type='text', name='search', id = "text_search", placeholder = "Search terms")
                  input(type='text', name='search', id = "amount_req", placeholder = "Amount required")
                  select.form-control
                      option Age
                      option 1
                      option 2
                      option 3
                      option 4
                      option 5
                  button.btn.btn-danger(type='submit') Search

    section#about.container.content-section.text-center
      .row
        .col-lg-8.col-lg-offset-2
          h2 Complete Database:
          ul
            - for (var i=0; i < funds.length; i++) {
              li= funds[i].id + " - " + funds[i].title + " - " + funds[i].genre
              li ------------------------
            - }
